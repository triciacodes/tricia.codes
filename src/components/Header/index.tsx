import React from 'react'

import * as styles from './styles.module.scss'

const Header = (): JSX.Element => {
  return <header className={styles.container}>This is the header.</header>
}

export default Header
